#include <iostream>
#include <vector>
#include <List>
using namespace std;

class Solution {
public:
	int maxEnvelopes(vector<vector<int>>& envelopes) {
		vector<vector<int>>::iterator w;
		vector<int>::iterator h;
		for (w=envelopes.begin(); w < envelopes.end(); w++)
		{

		}


	}
};

int main() {

	system("pause");
	return 0;
}